{
  "name": "terminalescape",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0",
    "npm": ">=10"
  },
  "scripts": {
    "validate:resources": "node scripts/validate-resources.mjs",
    "validate:commands": "node scripts/validate-commands.mjs",
    "dev": "vite",
    "build": "npm run validate:resources && npm run validate:commands && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.1.1"
  },
  "dependencies": {
    "pixi.js": "^8.19.0"
  }
}
